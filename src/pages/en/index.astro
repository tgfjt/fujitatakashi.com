---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { toSlug } from '../../lib/slug';

const entries = await getCollection('writing');
const recent = entries
  .filter((e) => !e.data.defunct)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 5);
---

<BaseLayout title="fujitatakashi.com" lang="en" altLangHref="/">
  <h1>fujitatakashi.com</h1>
  <p>Takashi Fujita's personal site.</p>

  <p>
    Most of the writing here is in Japanese, some in English. More English posts go to <a href="https://substack.com/@fujitatakashi" rel="noopener noreferrer" target="_blank">my Substack</a>.
  </p>

  <section>
    <h2>Recent writing</h2>
    <ul>
      {recent.map((entry) => (
        <li>
          <a href={`/writing/${toSlug(entry.id)}/`}>{entry.data.title}</a>
          <span class="date">{entry.data.date.toLocaleDateString('en-US')}</span>
        </li>
      ))}
    </ul>
    <p><a href="/writing/">See all</a></p>
  </section>
</BaseLayout>

<style>
  h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  section {
    margin-top: 3rem;
  }

  section h2 {
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  ul {
    list-style: none;
    padding: 0;
  }

  li {
    margin-bottom: 0.75rem;
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
  }

  .date {
    font-size: 0.875rem;
    opacity: 0.7;
    white-space: nowrap;
  }
</style>
