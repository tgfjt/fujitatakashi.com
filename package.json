{
  "name": "fujitatakashi-com",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4329",
    "build": "astro build && npx pagefind --site dist",
    "preview": "astro preview --port 4329",
    "deploy": "npm run build && wrangler deploy",
    "prepare": "panda codegen"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@pandacss/dev": "^0.52.0",
    "pagefind": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
